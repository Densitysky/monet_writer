# 2026-06-26

## 主题体系重构：3 风格 × 3 配色
- `AppThemeStyle` 从 5 值缩减为 3 值（modern, flat, golden），chinese/japanese 降级为配色方案
- 新增 `ColorPalette` 枚举：goldenAngle / chinese / japanese
- `_buildGoldenTheme` 接受 ColorPalette 参数分发三种 ColorScheme
- 删除 `StyleConfig.chinese` 和 `StyleConfig.japanese`，金色体系统一使用 `StyleConfig.golden`（字体格式一致）
- 新增 `effectivePalette` / `currentPaletteColors` / `allPaletteColors` 等便捷 getter
- 设置页 SegmentedButton 从 5 段改为 3 段，黄金风格下展开第二行配色选择

## 个人主页 Header 拖拽（二轮重做）
- **交互方式**：长按背景 → 进入浮动模式（震动 + 阴影 + 遮罩 + 指示条）→ 上下拖动调高度 → 松手磁吸退出
- 手势隔离：`onLongPressStart/MoveUpdate/End` 独占，不与 ListView 手势冲突
- **minHeight = 380**（改之前的固定值），**maxHeight = 动态计算**（数据统计/码字日历/回收站刚好在底部完整显示）
- **内容区全部固定**：头像 84px、headlineSmall 字号、22px 统计数字，Positioned(bottom:0) 钉死，不随高度缩放
- 背景 BoxFit.cover 自然裁切，消除变形
- 高度持久化到 SharedPreferences (key: profile_header_height)
- 封面裁剪比例 3:4（竖图）

## MediaQuery 时机修复
- BouncingWidget / FadeSlideEntrance: MediaQuery.of(context) 从 initState 移到 didChangeDependencies

## 封面双框预览 + 纵向偏移
- 裁剪完成后弹出预览窗，左右分栏显示 minHeight(380px) 和 maxHeight 效果
- Slider 调节 -1~+1 纵向偏移（控制 BoxFit.cover 的 alignment），40 级精度
- 偏移值持久化到 SharedPreferences (key: cover_alignment)

## 今日 UI 微调
- 底部导航上加分割线（左右 24px 边距）
- 三按钮贴近导航：底部间距 40→16，maxHeight 同步增加 24px
- 设置图标移回右上角 Positioned(top:0, right:0)
- 头像上方空白 60→30，统计行底部 padding 40→24
- 手势作用域缩小：GestureDetector 只包背景图，HitTestBehavior opaque→translucent
